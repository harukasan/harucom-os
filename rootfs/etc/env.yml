# Harucom OS environment variables.
# Loaded at boot by /system.rb and exposed via ENV[].
#
# KEYBOARD_LAYOUT: keyboard layout name. Corresponds to /lib/keymap/<name>.rb.
#                  Available: us, jis.
KEYBOARD_LAYOUT: us
